<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/WhiteTrans"
                android:centerY="1.0"
                android:endColor="@color/WhiteTrans"
                android:startColor="@color/WhiteTrans" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/LightGreen"
                    android:centerY="1.0"
                    android:endColor="@color/LightGreen"
                    android:startColor="@color/LightGreen" />
            </shape>
        </clip>
    </item>
</layer-list>