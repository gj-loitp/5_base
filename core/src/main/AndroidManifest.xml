<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="loitp.core">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Always required on api < 21, needed to keep a wake lock while your job is running -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Required on all api levels if you are using setPersisted(true) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name">
        <!-- support 18:9 -->
        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />
        <!--<receiver android:name="vn.loitp.core.utilities.LConectifyService">
            <intent-filter>
                &lt;!&ndash;<action android:name="android.net.wifi.STATE_CHANGE" />&ndash;&gt;
                &lt;!&ndash;<action android:name="android.net.conn.CONNECTIVITY_CHANGE" />&ndash;&gt;
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>-->

        <service
            android:name="vn.loitp.core.utilities.connection.LConectifyService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <service
            android:name="vn.loitp.views.uizavideo.view.floatview.FloatingUizaVideoService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="vn.loitp.core.loitp.adhelper.AdHelperActivity" />
        <activity android:name="vn.loitp.core.loitp.gallery.GalleryCoreSplashActivity" />
        <activity
            android:name="vn.loitp.core.loitp.gallery.album.GalleryCoreAlbumActivity"

            android:theme="@style/Theme.Swipe.Back.NoActionBar" />
        <activity
            android:name="vn.loitp.core.loitp.gallery.photos.GalleryCorePhotosActivity"
            android:theme="@style/Theme.Swipe.Back.NoActionBar" />
        <activity android:name="vn.loitp.core.loitp.gallery.slide.GalleryCoreSlideActivity" />
        <activity
            android:name="vn.loitp.core.loitp.facebookcomment.FacebookCommentActivity"
            android:theme="@style/Theme.Swipe.Back.NoActionBar" />

        <service android:name="vn.loitp.views.scrog.ScrogService" />
        <service
            android:name="vn.loitp.views.ldebugview.LDebugViewService"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name="vn.loitp.core.loitp.gallery.albumonly.GalleryCorePhotosOnlyActivity"
            android:theme="@style/Theme.Swipe.Back.NoActionBar" />
        <activity
            android:name="vn.loitp.core.loitp.gallery.member.GalleryMemberActivity"
            android:theme="@style/Theme.Swipe.Back.NoActionBar" />
        <activity
            android:name="vn.loitp.core.loitp.gallery.member.GalleryMemberDetailActivity"
            android:theme="@style/Theme.Swipe.Back.NoActionBar" />
    </application>

</manifest>
