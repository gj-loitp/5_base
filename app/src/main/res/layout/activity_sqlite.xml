<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/ll_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            android:id="@+id/bt_clear_all"
            style="@style/ButtonMenu"
            android:text="Clear all" />

        <Button
            android:id="@+id/bt_add"
            style="@style/ButtonMenu"
            android:text="Add Dummy Contact" />

        <Button
            android:id="@+id/btAdd100"
            style="@style/ButtonMenu"
            android:text="Add 100 Dummy Contact" />

        <Button
            android:id="@+id/bt_getcontact_with_id"
            style="@style/ButtonMenu"
            android:text="Get Contact With ID=2" />

        <Button
            android:id="@+id/btGetContactListPage1"
            style="@style/ButtonMenu"
            android:text="Get Contact List Page 1" />
    </LinearLayout>

    <ScrollView
        style="@style/ScrollVertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/ll_buttons"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@string/demo_sqlite_nclick_to_edit_long_click_to_delete"
                android:textColor="@color/Black"
                android:textSize="@dimen/txt_20" />

            <LinearLayout
                android:id="@+id/ll"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical" />
        </LinearLayout>
    </ScrollView>
</RelativeLayout>

