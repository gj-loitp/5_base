<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:id="@+id/scroll_view"
        style="@style/ScrollVertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/tvMenu"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/bt_sqlite_asset"
                style="@style/ButtonMenu"
                android:text="Read SQLite Asset" />

            <Button
                android:id="@+id/bt_realm"
                style="@style/ButtonMenu"
                android:text="Realm" />

            <Button
                android:id="@+id/btSharedPrefs"
                style="@style/ButtonMenu"
                android:text="SharedPrefs" />

            <Button
                android:id="@+id/btSharedPrefsEncryption"
                style="@style/ButtonMenu"
                android:text="SharedPrefsEncryption" />

            <Button
                android:id="@+id/bt_sqlite"
                style="@style/ButtonMenu"
                android:text="SQLite" />

            <Button
                android:id="@+id/btSqliteEncryption"
                style="@style/ButtonMenu"
                android:text="SQLite Encryption" />
        </LinearLayout>
    </ScrollView>

    <TextView
        android:id="@+id/tvMenu"
        style="@style/TextMenu"
        android:text="Menu Databse" />
</RelativeLayout>

